class EquationEvaluator {
    // ❌ Fragile string parsers prone to syntax parsing errors
    double evaluate(String expr, Map<String, Double> vars) {
        // Complex manual regex loops parsing operators sequentially
        return 0.0;
    }
}